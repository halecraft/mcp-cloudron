{
  "allOf": [
    {
      "$ref": "./domainBase.json"
    },
    {
      "type": "object",
      "properties": {
        "config": {
          "type": "object",
          "description": "Specifies config for Linode DNS.",
          "properties": {
            "token": {
              "type": "string",
              "description": "Token used for authentication with Linode DNS.",
              "example": "LiN0d3Dn5T0k3n1234567890"
            }
          },
          "required": [
            "token"
          ]
        }
      },
      "required": [
        "config"
      ]
    }
  ]
}